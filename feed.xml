<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://markkimotho.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://markkimotho.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-25T16:08:28+00:00</updated><id>https://markkimotho.github.io/feed.xml</id><title type="html">blank</title><subtitle>An odyssey for Mark Tinega </subtitle><entry><title type="html">Sketchbook: My Self-Hosted Drawing App</title><link href="https://markkimotho.github.io/blog/2026/sketchbook/" rel="alternate" type="text/html" title="Sketchbook: My Self-Hosted Drawing App"/><published>2026-03-22T10:00:00+00:00</published><updated>2026-03-22T10:00:00+00:00</updated><id>https://markkimotho.github.io/blog/2026/sketchbook</id><content type="html" xml:base="https://markkimotho.github.io/blog/2026/sketchbook/"><![CDATA[<p>I have loved art since I was young. Pencils, crayons, whatever I could get my hands on. I most especially loved drawing on old paper — notebooks, margins of textbooks, any blank surface available. As I got older that love shifted toward digital art. Layers, undo, infinite color. I spent a lot of time exploring what was possible.</p> <p>I was not very satisfied with the tools I came across though. So I decided to make my own. One that has that unbeatable look of old paper, rustic, and albeit being on a screen you can almost feel the roughness of the paper.</p> <p>It has taken a good while to get here. I have been working on it steadily, refining how the tools feel, getting the real-time collaboration solid, and making sure it holds up across different devices.</p> <p>Sketchbook is a self-hosted, real-time collaborative drawing app. You run it yourself, you own the data, and you can share a link with anyone to draw together on the same canvas.</p> <p>Here is what it supports right now: pencil, ink pen, brush, calligraphy, eraser, line, shapes, and a text tool. It has stylus and Apple Pencil support with real pressure sensitivity, and palm rejection so your hand resting on the iPad does not leave marks. There are multiple canvas surfaces — aged paper, kraft, watercolour, newsprint, old parchment, blackboard — plus ruled lines, grid, and dot grid overlays. It has layers, version history, and a chat panel for collaborating while drawing. It works on iPad, Android tablets, laptops, MacBooks, and phones.</p> <p>The whole thing runs on Next.js with a Socket.io server for real-time sync and SQLite for storage. No external database to configure, no cloud dependencies. Set it up on any machine in a few minutes.</p> <h2 id="getting-started">Getting started</h2> <p>Everything you need is on <a href="https://github.com/Markkimotho/digital-art-collaboration">GitHub</a>. Clone the repo, install dependencies, and start drawing:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/Markkimotho/digital-art-collaboration.git
<span class="nb">cd </span>digital-art-collaboration
npm <span class="nb">install
</span>npm run dev
</code></pre></div></div> <p>Open the <a href="http://localhost:3000">app locally</a> in your browser and you are good to go.</p> <h2 id="running-it-with-docker">Running it with Docker</h2> <p>If you do not want to deal with Node.js installs, Docker is the cleaner option. The image is built on Node 22 Alpine and everything is bundled inside — dependencies, the Next.js build, Prisma client, and the server. Canvas data persists in a Docker volume so it survives restarts.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># build the image</span>
docker build <span class="nt">-t</span> sketchbook:latest <span class="nb">.</span>

<span class="c"># run it</span>
docker compose up
</code></pre></div></div> <p>That is it. The app is at <code class="language-plaintext highlighter-rouge">http://localhost:3000</code>.</p> <h2 id="sharing-it-publicly">Sharing it publicly</h2> <p>Running it on your own machine is fine for personal use but if you want to share a link with someone on the internet you have a few options.</p> <p>The fastest is <a href="https://ngrok.com">ngrok</a>. While the container is running, open a second terminal and run:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ngrok http 3000
</code></pre></div></div> <p>ngrok hands you a public HTTPS link immediately, something like <code class="language-plaintext highlighter-rouge">https://abc123.ngrok-free.app</code>. Send that to anyone and they can open it in a browser and join your canvas. No domain, no server, no setup. Works well for sessions where you just want to draw with someone right now.</p> <p>For a permanent link you need a server. Spin up a cheap VPS — Hetzner, DigitalOcean, Linode, any of them work — install Docker, clone the repo, and run <code class="language-plaintext highlighter-rouge">docker compose up -d</code>. Point a domain at the server IP and put <a href="https://caddyserver.com">Caddy</a> in front of it for automatic HTTPS:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sketchbook.yourdomain.com {
    reverse_proxy localhost:3000
}
</code></pre></div></div> <p>Your public link becomes <code class="language-plaintext highlighter-rouge">https://sketchbook.yourdomain.com</code>. Anyone can open it, create a room, and share the room URL with collaborators.</p> <p>For a more production setup there are Kubernetes manifests in the repo. They cover the deployment, a persistent volume for the database, a ClusterIP service, nginx ingress with WebSocket headers, and cert-manager for automatic TLS certificates from Let’s Encrypt. You update the domain in the ingress file, push your image to a registry, apply the manifests with <code class="language-plaintext highlighter-rouge">kubectl apply -f k8s/</code>, and the cluster handles the rest.</p> <h2 id="how-to-use-it">How to use it</h2> <p>When you open the app you land on the lobby. Hit New Canvas and you get a fresh room. The URL in your browser is your room link — copy it and send it to whoever you want to draw with. They open the link and they are in the same canvas, live.</p> <p>The toolbar sits at the top on desktop and on the left side on mobile. Your tools are grouped into three sections: drawing tools, shapes, and utility tools. Pick a tool, pick a color from the color swatch or use the color picker for anything custom, adjust the stroke size, and start drawing. If you are on an iPad or a tablet with a stylus, pressure sensitivity works out of the box — no setup needed. Press lightly for thin strokes, press harder for thick ones.</p> <p>On the right side there is a panel with four tabs. Layers lets you add, rename, lock, and hide layers. Each layer is independent so you can sketch on one and ink on another without touching your base drawing. History is where you save named versions of your canvas. If you want to checkpoint your work before making big changes, save a version. If something goes wrong you can revert to any saved point and everyone in the room sees the canvas restore instantly.</p> <p>Canvas is where you change the surface and grid. You can switch between aged paper, kraft, watercolour paper, newsprint, old parchment, and blackboard. Underneath that you can toggle ruled lines, a square grid, or a dot grid — useful if you are doing anything that needs alignment. Chat is a simple message panel. Useful when you are collaborating and need to say something without leaving the canvas.</p> <p>The crosshair button at the bottom right of the canvas resets your view and fits everything you have drawn back into the screen. If you pan too far away and lose your work, that button brings you back. To pan around the canvas use the hand tool or two fingers on touch. To zoom, pinch on touch or scroll on desktop.</p> <p>If you have ideas or want to contribute, open a pull request. It is completely free and open. If you need help getting a hosted version running, reach out through <a href="https://github.com/Markkimotho/digital-art-collaboration">GitHub</a>.</p>]]></content><author><name></name></author><category term="projects"/><category term="projects"/><category term="art"/><category term="collaboration"/><category term="open-source"/><summary type="html"><![CDATA[A self-hosted, real-time collaborative drawing app built for artists who miss the feel of old paper.]]></summary></entry><entry><title type="html">Building a Production-Grade JSON Parser from Scratch</title><link href="https://markkimotho.github.io/blog/2025/building-production-grade-json-parser/" rel="alternate" type="text/html" title="Building a Production-Grade JSON Parser from Scratch"/><published>2025-09-25T14:30:00+00:00</published><updated>2025-09-25T14:30:00+00:00</updated><id>https://markkimotho.github.io/blog/2025/building-production-grade-json-parser</id><content type="html" xml:base="https://markkimotho.github.io/blog/2025/building-production-grade-json-parser/"><![CDATA[<p>I wanted to understand how parsing actually works. Not the high-level concepts, but the mechanics — how text becomes structured data. How a tokenizer identifies meaningful chunks. How a parser builds objects from those chunks. How errors get detected and reported.</p> <p>Most developers use the built-in <code class="language-plaintext highlighter-rouge">json</code> module without thinking about it. I wanted to know what was happening under the hood.</p> <h2 id="building-it">Building It</h2> <p>The goal was simple: understand tokenization, understand recursive descent parsing, understand the flow from raw text to Python objects. Everything else was secondary.</p> <p>I built it to:</p> <ul> <li>Learn how state machines work through a lexer</li> <li>Understand recursive descent parsing by implementing it</li> <li>See how position tracking enables useful error messages</li> <li>Handle the edge cases that make parsing tricky (escape sequences, number formats, nesting depth)</li> <li>Do it all in a single pass through the input</li> </ul> <p>Not to build the best parser. Not to optimize it. Just to understand.</p> <h2 id="the-architecture">The Architecture</h2> <p>The parser is split into two distinct phases: lexical analysis (the Lexer) and syntax analysis (the Parser).</p> <h3 id="lexer-tokenization">Lexer: Tokenization</h3> <p>The Lexer’s job is to convert a stream of characters into a stream of meaningful tokens. It reads the input sequentially and maintains a position pointer. At each position, it determines what type of token should be extracted.</p> <p><strong>The tokenization process:</strong></p> <ol> <li>Skip whitespace (spaces, tabs, newlines, carriage returns). Track line and column as you skip newlines.</li> <li>Read the next character. Determine what it is: <ul> <li>Structural tokens: <code class="language-plaintext highlighter-rouge">{</code>, <code class="language-plaintext highlighter-rouge">}</code>, <code class="language-plaintext highlighter-rouge">[</code>, <code class="language-plaintext highlighter-rouge">]</code>, <code class="language-plaintext highlighter-rouge">:</code>, <code class="language-plaintext highlighter-rouge">,</code> are single-character tokens. Return immediately.</li> <li>Literals: <code class="language-plaintext highlighter-rouge">true</code>, <code class="language-plaintext highlighter-rouge">false</code>, <code class="language-plaintext highlighter-rouge">null</code> are keyword tokens. Read ahead to verify the complete keyword.</li> <li>Strings: See a <code class="language-plaintext highlighter-rouge">"</code>. Read until the closing <code class="language-plaintext highlighter-rouge">"</code>, processing escape sequences along the way.</li> <li>Numbers: See a digit or minus sign. Read the complete number, validating format rules.</li> </ul> </li> <li>Return the token type and value.</li> </ol> <p><strong>String handling within the lexer is subtle.</strong> When you encounter a backslash inside a string, you must check the next character:</p> <ul> <li><code class="language-plaintext highlighter-rouge">\n</code> becomes a newline character</li> <li><code class="language-plaintext highlighter-rouge">\t</code> becomes a tab character</li> <li><code class="language-plaintext highlighter-rouge">\"</code> becomes a quote character</li> <li><code class="language-plaintext highlighter-rouge">\\</code> becomes a backslash character</li> <li><code class="language-plaintext highlighter-rouge">\u</code> followed by exactly 4 hex digits becomes a Unicode character</li> </ul> <p>If you see <code class="language-plaintext highlighter-rouge">\x</code>, that’s invalid — the lexer raises an error at that exact position. If you reach EOF before closing the string, error. If a newline appears inside a string (not escaped), error.</p> <p><strong>Number validation is precise.</strong> The JSON spec has specific rules:</p> <ul> <li>A number starts with an optional minus sign</li> <li>Then comes a digit sequence for the integer part <ul> <li><code class="language-plaintext highlighter-rouge">0</code> is valid by itself</li> <li><code class="language-plaintext highlighter-rouge">01</code> is invalid (no leading zeros except for <code class="language-plaintext highlighter-rouge">0</code>)</li> <li><code class="language-plaintext highlighter-rouge">123</code> is valid</li> </ul> </li> <li>Then optionally a decimal point followed by at least one digit (<code class="language-plaintext highlighter-rouge">1.5</code>, <code class="language-plaintext highlighter-rouge">0.001</code>) <ul> <li><code class="language-plaintext highlighter-rouge">1.</code> is invalid (no trailing decimal)</li> <li><code class="language-plaintext highlighter-rouge">.5</code> is invalid (no leading digit before decimal)</li> </ul> </li> <li>Then optionally an exponent: <code class="language-plaintext highlighter-rouge">e</code> or <code class="language-plaintext highlighter-rouge">E</code>, optional <code class="language-plaintext highlighter-rouge">+</code> or <code class="language-plaintext highlighter-rouge">-</code>, at least one digit <ul> <li><code class="language-plaintext highlighter-rouge">1e5</code> is valid</li> <li><code class="language-plaintext highlighter-rouge">1e+5</code> is valid</li> <li><code class="language-plaintext highlighter-rouge">1e</code> is invalid (no exponent digits)</li> <li><code class="language-plaintext highlighter-rouge">1e+</code> is invalid (no exponent digits)</li> </ul> </li> </ul> <p>The lexer validates all these rules as it reads. A malformed number is caught immediately.</p> <p><strong>Position tracking happens for every character.</strong> The lexer maintains <code class="language-plaintext highlighter-rouge">line</code> and <code class="language-plaintext highlighter-rouge">column</code> counters. When you read a regular character, increment <code class="language-plaintext highlighter-rouge">column</code>. When you read a newline, increment <code class="language-plaintext highlighter-rouge">line</code> and reset <code class="language-plaintext highlighter-rouge">column</code> to 0. This is why errors can report exact locations.</p> <h3 id="parser-recursive-descent">Parser: Recursive Descent</h3> <p>Once the Lexer produces tokens, the Parser consumes them and builds Python objects according to the JSON grammar.</p> <p><strong>The grammar (simplified):</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JSON      ::= value
value     ::= object | array | string | number | true | false | null
object    ::= '{' (pair (',' pair)*)? '}'
pair      ::= string ':' value
array     ::= '[' (value (',' value)*)? ']'
string    ::= STRING_TOKEN
number    ::= NUMBER_TOKEN
</code></pre></div></div> <p><strong>The implementation uses one function per grammar rule.</strong> Here’s the conceptual structure:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">parse</span><span class="p">():</span>
    <span class="c1"># Entry point. Parse a complete JSON value.
</span>    <span class="n">result</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">value</span><span class="p">()</span>
    <span class="c1"># Must be at EOF now. If there's extra tokens, error.
</span>    <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">current_token</span> <span class="o">!=</span> <span class="n">EOF</span><span class="p">:</span>
        <span class="k">raise</span> <span class="nc">JSONParseError</span><span class="p">(</span><span class="sh">"</span><span class="s">Unexpected token after value</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">result</span>

<span class="k">def</span> <span class="nf">value</span><span class="p">():</span>
    <span class="c1"># Try to parse any valid JSON value.
</span>    <span class="k">if</span> <span class="nf">matches</span><span class="p">(</span><span class="sh">'</span><span class="s">{</span><span class="sh">'</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">object</span><span class="p">()</span>
    <span class="k">elif</span> <span class="nf">matches</span><span class="p">(</span><span class="sh">'</span><span class="s">[</span><span class="sh">'</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">array</span><span class="p">()</span>
    <span class="k">elif</span> <span class="nf">matches</span><span class="p">(</span><span class="n">STRING</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">string</span><span class="p">()</span>
    <span class="k">elif</span> <span class="nf">matches</span><span class="p">(</span><span class="n">NUMBER</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">number</span><span class="p">()</span>
    <span class="k">elif</span> <span class="nf">matches</span><span class="p">(</span><span class="sh">'</span><span class="s">true</span><span class="sh">'</span><span class="p">):</span>
        <span class="k">return</span> <span class="bp">True</span>
    <span class="k">elif</span> <span class="nf">matches</span><span class="p">(</span><span class="sh">'</span><span class="s">false</span><span class="sh">'</span><span class="p">):</span>
        <span class="k">return</span> <span class="bp">False</span>
    <span class="k">elif</span> <span class="nf">matches</span><span class="p">(</span><span class="sh">'</span><span class="s">null</span><span class="sh">'</span><span class="p">):</span>
        <span class="k">return</span> <span class="bp">None</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">raise</span> <span class="nc">JSONParseError</span><span class="p">(</span><span class="sh">"</span><span class="s">Expected a JSON value</span><span class="sh">"</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">object</span><span class="p">():</span>
    <span class="c1"># Parse { pair, pair, ... }
</span>    <span class="nf">consume</span><span class="p">(</span><span class="sh">'</span><span class="s">{</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">result</span> <span class="o">=</span> <span class="p">{}</span>
    <span class="k">if</span> <span class="ow">not</span> <span class="nf">matches</span><span class="p">(</span><span class="sh">'</span><span class="s">}</span><span class="sh">'</span><span class="p">):</span>
        <span class="c1"># At least one pair
</span>        <span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
            <span class="n">key</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">string</span><span class="p">()</span>  <span class="c1"># Key must be a string
</span>            <span class="nf">consume</span><span class="p">(</span><span class="sh">'</span><span class="s">:</span><span class="sh">'</span><span class="p">)</span>
            <span class="n">val</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">value</span><span class="p">()</span>   <span class="c1"># Value can be anything
</span>
            <span class="c1"># Check for duplicate keys
</span>            <span class="k">if</span> <span class="n">key</span> <span class="ow">in</span> <span class="n">result</span><span class="p">:</span>
                <span class="k">raise</span> <span class="nc">JSONParseError</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Duplicate key </span><span class="sh">'</span><span class="si">{</span><span class="n">key</span><span class="si">}</span><span class="sh">'"</span><span class="p">)</span>
            <span class="n">result</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">=</span> <span class="n">val</span>

            <span class="k">if</span> <span class="ow">not</span> <span class="nf">matches</span><span class="p">(</span><span class="sh">'</span><span class="s">,</span><span class="sh">'</span><span class="p">):</span>
                <span class="k">break</span>
            <span class="nf">consume</span><span class="p">(</span><span class="sh">'</span><span class="s">,</span><span class="sh">'</span><span class="p">)</span>
            <span class="c1"># Check for trailing comma
</span>            <span class="k">if</span> <span class="nf">matches</span><span class="p">(</span><span class="sh">'</span><span class="s">}</span><span class="sh">'</span><span class="p">):</span>
                <span class="k">raise</span> <span class="nc">JSONParseError</span><span class="p">(</span><span class="sh">"</span><span class="s">Trailing comma in object</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">consume</span><span class="p">(</span><span class="sh">'</span><span class="s">}</span><span class="sh">'</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">result</span>

<span class="k">def</span> <span class="nf">array</span><span class="p">():</span>
    <span class="c1"># Parse [ value, value, ... ]
</span>    <span class="nf">consume</span><span class="p">(</span><span class="sh">'</span><span class="s">[</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">result</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">if</span> <span class="ow">not</span> <span class="nf">matches</span><span class="p">(</span><span class="sh">'</span><span class="s">]</span><span class="sh">'</span><span class="p">):</span>
        <span class="c1"># At least one value
</span>        <span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
            <span class="n">result</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">value</span><span class="p">())</span>
            <span class="k">if</span> <span class="ow">not</span> <span class="nf">matches</span><span class="p">(</span><span class="sh">'</span><span class="s">,</span><span class="sh">'</span><span class="p">):</span>
                <span class="k">break</span>
            <span class="nf">consume</span><span class="p">(</span><span class="sh">'</span><span class="s">,</span><span class="sh">'</span><span class="p">)</span>
            <span class="c1"># Check for trailing comma
</span>            <span class="k">if</span> <span class="nf">matches</span><span class="p">(</span><span class="sh">'</span><span class="s">]</span><span class="sh">'</span><span class="p">):</span>
                <span class="k">raise</span> <span class="nc">JSONParseError</span><span class="p">(</span><span class="sh">"</span><span class="s">Trailing comma in array</span><span class="sh">"</span><span class="p">)</span>
    <span class="nf">consume</span><span class="p">(</span><span class="sh">'</span><span class="s">]</span><span class="sh">'</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">result</span>
</code></pre></div></div> <p>The key insight: each function reads tokens, calls other functions for sub-expressions, checks for valid structure, and returns the built object.</p> <p><strong>Depth tracking prevents stack overflow.</strong> The parser maintains a <code class="language-plaintext highlighter-rouge">current_depth</code> counter:</p> <ul> <li>Increment when entering an object or array</li> <li>Decrement when exiting</li> <li>Raise an error if depth exceeds the limit (default 1000)</li> </ul> <p>This guards against malicious or accidental deeply nested structures that would exhaust the call stack.</p> <p><strong>Error reporting includes context.</strong> When parsing fails, the error includes:</p> <ul> <li>The error message (what was wrong)</li> <li>The line number (from the lexer’s position tracking)</li> <li>The column number (from the lexer’s position tracking)</li> <li>The token that caused the failure</li> </ul> <p>This makes debugging large JSON files practical.</p> <h3 id="the-flow-from-text-to-object">The Flow: From Text to Object</h3> <p>Let’s trace an example: <code class="language-plaintext highlighter-rouge">{"name": "Alice", "age": 30}</code></p> <p><strong>Lexer output (tokens):</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{ (LBRACE)
"name" (STRING: "name")
: (COLON)
"Alice" (STRING: "Alice")
, (COMMA)
"age" (STRING: "age")
: (COLON)
30 (NUMBER: 30)
} (RBRACE)
</code></pre></div></div> <p><strong>Parser execution:</strong></p> <ol> <li><code class="language-plaintext highlighter-rouge">parse()</code> calls <code class="language-plaintext highlighter-rouge">value()</code></li> <li><code class="language-plaintext highlighter-rouge">value()</code> sees <code class="language-plaintext highlighter-rouge">{</code>, calls <code class="language-plaintext highlighter-rouge">object()</code></li> <li><code class="language-plaintext highlighter-rouge">object()</code> consumes <code class="language-plaintext highlighter-rouge">{</code></li> <li>Sees <code class="language-plaintext highlighter-rouge">"name"</code> (not <code class="language-plaintext highlighter-rouge">}</code>), so enter the pair loop</li> <li><code class="language-plaintext highlighter-rouge">string()</code> returns <code class="language-plaintext highlighter-rouge">"name"</code></li> <li>Consumes <code class="language-plaintext highlighter-rouge">:</code></li> <li><code class="language-plaintext highlighter-rouge">value()</code> returns <code class="language-plaintext highlighter-rouge">"Alice"</code></li> <li>Add to result: <code class="language-plaintext highlighter-rouge">{"name": "Alice"}</code></li> <li>Sees <code class="language-plaintext highlighter-rouge">,</code>, consume it</li> <li>Loop continues</li> <li><code class="language-plaintext highlighter-rouge">string()</code> returns <code class="language-plaintext highlighter-rouge">"age"</code></li> <li>Consumes <code class="language-plaintext highlighter-rouge">:</code></li> <li><code class="language-plaintext highlighter-rouge">value()</code> returns <code class="language-plaintext highlighter-rouge">30</code></li> <li>Add to result: <code class="language-plaintext highlighter-rouge">{"name": "Alice", "age": 30}</code></li> <li>Sees <code class="language-plaintext highlighter-rouge">}</code> (not <code class="language-plaintext highlighter-rouge">,</code>), exit loop</li> <li>Consumes <code class="language-plaintext highlighter-rouge">}</code></li> <li>Return <code class="language-plaintext highlighter-rouge">{"name": "Alice", "age": 30}</code></li> <li>Back in <code class="language-plaintext highlighter-rouge">parse()</code>, check if at EOF. Yes. Return result.</li> </ol> <p><strong>Python object:</strong> <code class="language-plaintext highlighter-rouge">{'name': 'Alice', 'age': 30}</code></p> <h3 id="error-cases">Error Cases</h3> <p><strong>Example 1: Duplicate key</strong></p> <p>Input: <code class="language-plaintext highlighter-rouge">{"key": 1, "key": 2}</code></p> <p>Parser reaches the second <code class="language-plaintext highlighter-rouge">"key"</code>. In the <code class="language-plaintext highlighter-rouge">object()</code> function’s loop, it checks <code class="language-plaintext highlighter-rouge">if key in result:</code> and finds it already exists. Raises <code class="language-plaintext highlighter-rouge">JSONParseError("Duplicate key 'key'")</code></p> <p><strong>Example 2: Trailing comma</strong></p> <p>Input: <code class="language-plaintext highlighter-rouge">[1, 2, 3,]</code></p> <p>Parser in <code class="language-plaintext highlighter-rouge">array()</code> loop. After parsing <code class="language-plaintext highlighter-rouge">3</code>, sees <code class="language-plaintext highlighter-rouge">,</code> and consumes it. Next iteration, <code class="language-plaintext highlighter-rouge">value()</code> checks if next token is <code class="language-plaintext highlighter-rouge">]</code>. It is. But the rule says after a comma there must be another value, not the closing bracket. Raises <code class="language-plaintext highlighter-rouge">JSONParseError("Trailing comma in array")</code></p> <p><strong>Example 3: Invalid escape</strong></p> <p>Input: <code class="language-plaintext highlighter-rouge">"hello\xworld"</code></p> <p>Lexer in string parsing. Sees <code class="language-plaintext highlighter-rouge">\</code>, checks next character. It’s <code class="language-plaintext highlighter-rouge">x</code>, which is not a valid escape character. Raises <code class="language-plaintext highlighter-rouge">JSONParseError("Invalid escape sequence: \x")</code> at the exact location of the <code class="language-plaintext highlighter-rouge">\x</code>.</p> <p><strong>Example 4: Leading zero</strong></p> <p>Input: <code class="language-plaintext highlighter-rouge">{"count": 01}</code></p> <p>Lexer reading the number. Sees <code class="language-plaintext highlighter-rouge">0</code>. Checks next character. It’s <code class="language-plaintext highlighter-rouge">1</code>, not <code class="language-plaintext highlighter-rouge">.</code> or <code class="language-plaintext highlighter-rouge">e</code> or a structural token. This means <code class="language-plaintext highlighter-rouge">01</code> which violates the no-leading-zeros rule. Raises <code class="language-plaintext highlighter-rouge">JSONParseError("Invalid number format")</code></p> <h2 id="what-it-supports">What It Supports</h2> <p>The parser handles all JSON according to spec:</p> <ul> <li><strong>Primitives</strong>: null, booleans, numbers, strings</li> <li><strong>Collections</strong>: objects and arrays, with arbitrary nesting</li> <li><strong>Numbers</strong>: integers, floats, scientific notation (1e5, 1.5e-3)</li> <li><strong>Escapes</strong>: All standard escape sequences including <code class="language-plaintext highlighter-rouge">\uXXXX</code> for Unicode</li> <li><strong>Validation</strong>: Detects leading zeros, duplicate keys, invalid escapes, trailing commas</li> <li><strong>Limits</strong>: Recursion depth limiting and file size validation to prevent pathological cases</li> </ul> <p>The interesting part was not the feature list but <em>how</em> it works.</p> <h2 id="what-i-learned">What I Learned</h2> <p><strong>Tokenization is meticulous.</strong> Splitting text into tokens seems simple until you try it. You need to handle:</p> <ul> <li>Whitespace across lines (tracking line/column for every character)</li> <li>Strings with embedded escapes (checking that <code class="language-plaintext highlighter-rouge">\uXXXX</code> is exactly 4 hex digits, not <code class="language-plaintext highlighter-rouge">\u000G</code>)</li> <li>Numbers with optional components (the integer part is required, but decimal and exponent are optional)</li> <li>Keywords that must be exact (it’s <code class="language-plaintext highlighter-rouge">null</code>, not <code class="language-plaintext highlighter-rouge">nul</code> or <code class="language-plaintext highlighter-rouge">NULL</code>)</li> </ul> <p>One edge case I didn’t expect: what counts as whitespace? JSON allows <code class="language-plaintext highlighter-rouge">\t</code>, <code class="language-plaintext highlighter-rouge">\n</code>, <code class="language-plaintext highlighter-rouge">\r</code>, and space. But not all Unicode whitespace. The spec is specific. If you’re lenient, you silently accept invalid JSON.</p> <p><strong>Recursive descent parsing is beautifully self-documenting.</strong> The code <em>is</em> the grammar. When someone reads <code class="language-plaintext highlighter-rouge">def object():</code> they see how objects are structured without referring to documentation. The recursive calls (<code class="language-plaintext highlighter-rouge">key = self.string()</code>, <code class="language-plaintext highlighter-rouge">val = self.value()</code>) showed the exact nesting. But it requires discipline: every grammar rule must be a function, and every function must follow the same pattern: consume expected tokens, handle the payload, validate structure.</p> <p><strong>Position tracking is not optional.</strong> I first built the parser without it. Errors said “invalid JSON” somewhere. Useless. Adding line/column tracking meant modifying every <code class="language-plaintext highlighter-rouge">consume()</code> call, every token-reading operation, and maintaining <code class="language-plaintext highlighter-rouge">line</code>/<code class="language-plaintext highlighter-rouge">column</code> during whitespace skipping. But then errors became “line 42, column 5: duplicate key”. That’s the difference between “something is wrong” and “here’s the problem.”</p> <p><strong>Duplicate key detection needs O(n) lookups.</strong> For each pair added to an object, checking if the key already exists requires looking it up. With n pairs, that’s n lookups, each O(1) in a dict. Total O(n) which is acceptable. But I had to decide: should the parser allow duplicates and let the caller decide? Or reject them? The JSON spec says objects are unordered collections of key-value pairs, implying uniqueness. I made the decision to reject duplicates immediately.</p> <p><strong>Number parsing is surprisingly complex for such a simple-looking problem.</strong> Python’s <code class="language-plaintext highlighter-rouge">int()</code> and <code class="language-plaintext highlighter-rouge">float()</code> handle many formats, but the JSON spec is strict. <code class="language-plaintext highlighter-rouge">01</code> is invalid. <code class="language-plaintext highlighter-rouge">1.</code> is invalid. <code class="language-plaintext highlighter-rouge">e5</code> is invalid. You could try parsing and catching exceptions, but that mixes concerns. Better to validate the format before parsing, so you can report the exact error.</p> <p><strong>The call stack is a resource.</strong> Recursive descent parsing builds a call stack as deep as the nesting depth. For JSON with objects containing arrays containing objects containing arrays… the stack grows. The default recursion limit in Python is ~1000. A pathological JSON file with 10000 levels of nesting would crash. But most real-world JSON is shallower. Still, tracking depth and failing fast when a limit is hit is more graceful than a stack overflow.</p> <p><strong>Trailing commas are a special case.</strong> They’re syntactically simple to detect: after you parse a value and consume a comma, check if the next token is the closing bracket/brace. If so, error. If not, continue parsing the next value. But I had to remember to check in both arrays and objects. A helper function for this would be cleaner, but the duplication made the logic explicit.</p> <p><strong>Tests revealed assumptions I made wrong.</strong> Building the parser, I assumed certain things about valid JSON:</p> <ul> <li>Empty objects <code class="language-plaintext highlighter-rouge">{}</code> are valid (yes)</li> <li>Empty arrays <code class="language-plaintext highlighter-rouge">[]</code> are valid (yes)</li> <li>Objects can have zero pairs (yes, the optional part of the grammar)</li> <li>Whitespace before/after values doesn’t matter (yes, and the lexer must skip it)</li> <li><code class="language-plaintext highlighter-rouge">null</code> is different from empty string <code class="language-plaintext highlighter-rouge">""</code> (yes, very different)</li> </ul> <p>Each assumption got tested. Most were correct. The few that weren’t got fixed. That iterative refinement is where the real learning happened.</p> <h2 id="validation">Validation</h2> <p>I tested it thoroughly to make sure it actually works. 38 unit tests cover the lexer (tokenization), the parser (grammar and structure validation), and error handling. Another 40 JSON test files across different categories ensure edge cases are handled.</p> <p>All tests pass. The parser correctly accepts all valid JSON and correctly rejects all invalid JSON.</p> <p>Test coverage includes:</p> <ul> <li>Lexer: All token types, escape sequences, number formats, whitespace</li> <li>Parser: Objects, arrays, nesting, mixed types, error cases</li> <li>Edge cases: Deep nesting, large numbers, special characters, trailing commas, duplicate keys</li> </ul> <p>The testing revealed the gaps in my thinking. A test would fail, I’d add handling for that case, and suddenly the parser got more robust. That’s the learning in action.</p> <h2 id="wrap-up">Wrap Up</h2> <p>I understand parsing now. Not as a black box. Not as “the JSON library handles it.” But as a concrete process: read characters, group them into tokens, use grammar rules to build structure, validate as you go.</p> <p>The parser works. It’s about 400 lines of Python. It handles the JSON spec. It’s tested.</p> <p>But the real point was the understanding. Once you build one, you see parsing everywhere. Configuration files, markup languages, DSLs — they all follow the same pattern. Tokenize, parse, validate.</p> <p>The code is on GitHub if you want to see it. But more importantly: if you want to understand parsing, consider building one yourself. Don’t use an existing parser. Build it. You’ll learn more from the failures and edge cases than from reading about it.</p>]]></content><author><name></name></author><category term="projects"/><category term="projects"/><category term="json"/><category term="parser"/><category term="parsing"/><category term="open-source"/><category term="python"/><category term="flask"/><summary type="html"><![CDATA[A robust, scalable JSON parser written in Python with comprehensive error handling, full JSON spec support, and a web interface for real-time validation.]]></summary></entry></feed>